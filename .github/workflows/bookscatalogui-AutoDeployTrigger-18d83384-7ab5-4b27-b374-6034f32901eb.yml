name: Trigger auto deployment for bookscatalogui

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/bookscatalogui-AutoDeployTrigger-18d83384-7ab5-4b27-b374-6034f32901eb.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.BOOKSCATALOGUI_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.BOOKSCATALOGUI_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.BOOKSCATALOGUI_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: democontregistries.azurecr.io
          registryUsername: ${{ secrets.BOOKSCATALOGUI_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.BOOKSCATALOGUI_REGISTRY_PASSWORD }}
          containerAppName: bookscatalogui
          resourceGroup: rg-container-apps
          imageToBuild: democontregistries.azurecr.io/bookscatalogui:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


